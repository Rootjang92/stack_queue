function Queue_stack() {
  var stack1 = [];
  var stack2 = [];

  this.enqueue = function(element) {
    stack1.push(element);
  };

  this.dequeue = function() {
    if (stack2.length === 0) {

    }
  }
}